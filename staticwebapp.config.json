{
  "routes": [
    {
      "route": "/*",
      "rewrite": "/[tenant]",
      "exclude": ["/_next/*"]
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": ["/images/*.{png,jpg,gif}", "/css/*"]
  }
}
